# Gate de Fase 2 — aprobado

Fecha: 2026-08-03

## Resultado

La fase PostgreSQL/JPA/Flyway queda aprobada para el hito académico.

| Criterio | Evidencia | Estado |
|---|---|---|
| Base vacía | Flyway V1–V5 sobre PostgreSQL 16.14 | Aprobado |
| JPA | Spring Boot inicia con `ddl-auto=validate` | Aprobado |
| Versiones únicas | Cinco migraciones versionadas sin duplicados | Aprobado |
| RLS | `regula_app` solo observa el tenant configurado | Aprobado |
| Upgrade | Migración automatizada V4→V5 | Aprobado |
| Backup/restore | `pg_dump` y `pg_restore` conservan esquema, historial y datos | Aprobado |
| Seeds | Producción no carga demo; perfil demo carga repeatable aislada | Aprobado |
| Roles | Roles de grupo en V5 y logins mediante script sin contraseñas embebidas | Aprobado |
| Runtime | Suite ejecutada con Eclipse Temurin Java 17.0.20 | Aprobado |
| Suite | 18 pruebas, 0 fallos, 0 errores | Aprobado |

## Artefactos operativos

- `application-prod.yml`: exige secretos y credenciales mediante variables.
- `application-demo.yml`: añade exclusivamente `db/demo`.
- `.env.example`: nombres de variables sin secretos utilizables.
- `scripts/provision-postgresql-roles.sql`: crea logins de mínimo privilegio.
- `scripts/backup.ps1` y `scripts/restore.ps1`: operación manual reproducible.
- `MATRIZ_COMPATIBILIDAD_ESQUEMA.md`: política de upgrade y roll-forward.

## Siguiente gate

La Fase 3 debe aportar OpenAPI versionado, mock HTTPS independiente, clientes tipados, truststore, resiliencia, correlation ID, auditoría sanitizada y contract tests.

